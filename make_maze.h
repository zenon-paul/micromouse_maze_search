#pragma once
/*#include"container.h"
#define HIGHT 65
#define WIDTH 65

#define WALL 3

class Maze {
private:
	d2_prqueue p_info2;
	int direction_set_1[4][2];
	int random_num;
	void listup_available_point(int list___num_ps[WIDTH * HIGHT], int list___num_ps_index[WIDTH * HIGHT], int lim);
	void random_select(int list___num_ps_index[WIDTH * HIGHT], int* start, int count);
	void turn_list_to_1(int list___num_ps[WIDTH * HIGHT], int list___num_ps_index[WIDTH * HIGHT], int start, int lim);
	void check_dead_end_even(int s_body[WIDTH * HIGHT], int start, int dire4[4], int leng);
	void from_component_to_index_even(int x, int y, int* index);
	void from_index_to_component_even(int i, int* x, int* y);
public:
	int** bord1;
	Maze();
	~Maze();
	void init_for_extending_wall();
	void make_maze_easier();
	void extending_wall();
};*/
